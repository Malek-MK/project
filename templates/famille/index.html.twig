{% extends 'template1.html.twig' %}

{% block title %}Liste Famille{% endblock %}

{% block body %}
 <div class="container">
    <div class="panel panel-primary">
    <div class="panel-heading">
    <i ></i><h2>Liste Famille</h2>
</div>
<div class="panel-body">

    <table class="table">
        <thead class="thead-dark">
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for famille in familles %}
            <tr>
                <td>{{ famille.id }}</td>
                <td>{{ famille.libelle }}</td>
                <td>
                    <a href="{{ path('famille_show', {'id': famille.id}) }}"class="btn btn-success">show</a>
                   <br>
                    <a href="{{ path('famille_edit', {'id': famille.id}) }}"class="btn btn-warning">edit</a>
                    {{ include('famille/_delete_form.html.twig') }}

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('famille_new') }}" class="btn btn-info">Create new</a>
    </div>
    </div>
    </div>
{% endblock %}